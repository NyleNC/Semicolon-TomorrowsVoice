@model IEnumerable<TomorrowsVoices.Models.Singer>
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" />
<link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">

@{
    ViewData["Title"] = "Singers";
    int index = 1;
}

<h1>Singers</h1>

<div class="d-flex justify-content-between align-items-end mb-3">
    <div class="text-center">
        <a asp-action="Create" class="btn btn-primary">Add Singer</a>
    </div>
    <div>
        <div class="text-danger">@Html.Raw(TempData["Feedback"])</div>

        <form asp-action="InsertFromExcel" asp-controller="Singer" method="post" enctype="multipart/form-data">
            <div class="card-body p-0" >
                <div class="d-flex justify-content-between align-items-end">
                    <div>
                        <label class="control-label custom-file-label">Upload Excel File:</label>
                        <input type="file" class="form-control" name="theExcel" id="theExcel" style="max-width: 300px;">
                    </div>
                    <input type="submit" value="Upload Data" class="btn btn-dark ms-2" />
                </div>
            </div>
        </form>
    </div>
</div>


<form asp-action="Index" method="get">
    <input type="hidden" name="sortDirection" value="@ViewData["sortDirection"]" />
    <input type="hidden" name="sortField" value="@ViewData["sortField"]" />

    <table class="table table-hover">
        <thead class="table-dark">
            <tr>
                <th class="d-none d-md-table-cell">
                    <button type="button" name="actionButton" value="#" class="btn btn-toolbar text-white"> #</button>
                </th>
                <th>
                    <button type="button" name="actionButton" value="Singer" class="btn btn-toolbar text-white" onclick="handleSort(event, 'Singer')">
                        Singer
                        @if (ViewData["sortField"]?.ToString() == "Singer")
                        {
                            if (ViewData["sortDirection"]?.ToString() == "asc")
                            {
                                <i class="bi bi-sort-alpha-up ms-2"></i>
                            }
                            else
                            {
                                <i class="bi bi-sort-alpha-down ms-2"></i>
                            }
                        }
                    </button>
                </th>
                <th class="d-none d-md-table-cell">
                    <button type="button" value="Location" class="btn btn-toolbar text-white">Location</button>
                </th>
                <th class="d-none d-md-table-cell">
                    <button type="button" name="actionButton" value="Availability" class="btn btn-toolbar text-white">Availability</button>
                </th>
                <th class="d-none d-md-table-cell">
                    <button type="button" name="actionButton" value="CreatedAt" class="btn btn-toolbar text-white">Created At</button>
                </th>
                <th class="d-none d-md-table-cell">
                    <button type="button" name="actionButton" value="UpdatedAt" class="btn btn-toolbar text-white">Updated At</button>
                </th>
                <th class="d-none d-md-table-cell">
                    <button type="button" name="actionButton" value="Actions" class="btn btn-toolbar text-white">Actions</button>
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@index</td>
                    <td>@Html.DisplayFor(modelItem => item.FullName)</td>
                    <td>@(item.Location != null ? item.Location.City : "Not Set")</td>
                    <td>
                        <span style="color:black" class="badge @(item.IsAvailable ? "badge-success" : "badge-danger")">
                            @(item.IsAvailable ? "Available" : "Not Available")
                        </span>
                    </td>
                    <td>@item.CreatedAt.ToString("yyyy-MM-dd HH:mm")</td>
                    <td>
                        @(item.UpdatedAt.HasValue ? item.UpdatedAt.Value.ToString("yyyy-MM-dd HH:mm") : "Never Updated")
                    </td>
                    <td>
                        <a asp-action="Edit" asp-route-id="@item.ID" class="btn btn-sm btn-warning">Edit</a> |
                        <a asp-action="Details" asp-route-id="@item.ID" class="btn btn-sm btn-info">Details</a> |
                        <a asp-action="Delete" asp-route-id="@item.ID" class="btn btn-sm btn-danger">Delete</a> |
                        <form asp-action="ToggleAvailability" asp-route-id="@item.ID" method="post" style="display:inline;">
                            <button type="submit" class="btn btn-sm btn-secondary">
                                @(item.IsAvailable ? "Mark as Unavailable" : "Mark as Available")
                            </button>
                        </form>
                    </td>
                </tr>
                index++;
            }
        </tbody>
    </table>
</form>
 
<script>
    function handleSort(event, field) {
        event.preventDefault();
        const form = event.target.closest('form');
        const sortField = form.querySelector('input[name="sortField"]');
        const sortDirection = form.querySelector('input[name="sortDirection"]');

        if (sortField.value === field) {
            // Toggle sort direction
            sortDirection.value = sortDirection.value === "asc" ? "desc" : "asc";
        } else {
            // Set new field and default to ascending order
            sortField.value = field;
            sortDirection.value = "asc";
        }

        // Submit the form
        form.submit();
    }
</script>
